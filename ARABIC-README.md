# 🚀 كيفية تشغيل موقع KIDE

## ⚠️ مهم جداً: لماذا لا يعمل بالنقر المزدوج؟

عندما تفتح ملف `dist/index.html` مباشرة في Safari، تظهر صفحة بيضاء لأن:

1. **React يستخدم ES Modules** - Safari يمنع تشغيل ES Modules من ملفات محلية لأسباب أمنية (CORS Policy)
2. **المشروع يحتاج خادم** - حتى مع المسارات النسبية، JavaScript Modules تحتاج HTTP Server

## ✅ الحلول الصحيحة:

### الحل 1: استخدام ملف التشغيل السريع (الأسهل) 🎯

1. اضغط مرتين على ملف `start-kide.command`
2. اختر رقم 2 (خادم محلي)
3. افتح المتصفح على: `http://localhost:8000`

### الحل 2: من Terminal

```bash
# الطريقة 1: خادم التطوير
npm run dev
# ثم افتح: http://localhost:5173

# الطريقة 2: عرض النسخة المبنية
npm run build
python3 -m http.server 8000 --directory dist
# ثم افتح: http://localhost:8000
```

### الحل 3: استخدام Live Server (VS Code)

1. افتح المشروع في VS Code
2. انقر بالزر الأيمن على `dist/index.html`
3. اختر "Open with Live Server"

## 📋 الأوامر المتاحة:

| الأمر | الوصف |
|-------|-------|
| `npm run dev` | تشغيل خادم التطوير (للتعديل) |
| `npm run build` | بناء المشروع للإنتاج |
| `npm run preview` | معاينة النسخة المبنية |

## 🔧 حل مشكلة الصفحة البيضاء:

### السبب الرئيسي:
Safari وChrome يمنعان تشغيل ES Modules من بروتوكول `file://` لأسباب أمنية.

### الخطأ في Console:
```
Access to script at 'file:///.../index.js' from origin 'null' has been blocked by CORS policy
```

### الحل:
**لا يمكن فتح الملف مباشرة** - يجب استخدام خادم HTTP (حتى لو كان محلياً)

## 🎓 للتسليم للمعلم:

إذا أراد المعلم تشغيل المشروع:

### الطريقة 1: رابط GitHub
```
https://github.com/FAHAD11ABBAS/Kirjallisuuden-edist-miskeskus
```

### الطريقة 2: تعليمات التشغيل
أعطه هذه الخطوات:
1. تحميل المشروع
2. فتح Terminal
3. تشغيل:
   ```bash
   npm install
   npm run dev
   ```
4. فتح المتصفح على: `http://localhost:5173`

## 💡 نصائح:

- ✅ **استخدم دائماً خادم** لتشغيل React
- ✅ **`npm run dev`** للتطوير
- ✅ **`python3 -m http.server`** لعرض سريع
- ❌ **لا تفتح HTML مباشرة** في Safari/Chrome

## 🆘 إذا واجهت مشاكل:

```bash
# إعادة تثبيت المكتبات
rm -rf node_modules package-lock.json
npm install

# تنظيف وإعادة البناء
rm -rf dist
npm run build
```
